{
  "scripts": {
    "dev": "rimraf dist && parcel index/index.html packages/index.html"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.1.0",
    "rimraf": "^3.0.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}